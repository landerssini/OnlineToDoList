import { Router } from '../Router/Router'
import { Toaster } from 'react-hot-toast'

export const Interface = () => {
    return (
        <>
            <Toaster />
            <Router />
        </>
    )
}
